<% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
        <div class="alert alert-<%= name == :notice ? "success" : "warning" %>" role="alert">
            <%= content_tag :div, msg %>
        </div>
    <% end %>
<% end %>